# Core Framework
streamlit
# Text-to-Speech Engine
pyttsx3
sympy
# Alternative TTS engines (fallback options)
#   pywin32
gtts # Google Text-to-Speech (requires internet)
requests  # Required for gtts
# playsound>=1.3.0  # Simple audio playback (cross-platform) - commented out due to build issues
pythonturtle
pyopenpyxl
